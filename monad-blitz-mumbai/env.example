# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL connection string (Neon, Supabase, or local PostgreSQL)
# Format: postgresql://username:password@host:port/database_name
# Example for Neon: postgresql://user:pass@ep-xxx.us-east-1.aws.neon.tech/dbname?sslmode=require
DATABASE_URL="postgresql://username:password@localhost:5432/cryptobazaar"

# ============================================
# POLYGON AMOY TESTNET CONFIGURATION
# ============================================
# Chain ID for Polygon Amoy testnet
NEXT_PUBLIC_CHAIN_ID=80002

# RPC URL for Polygon Amoy testnet
NEXT_PUBLIC_RPC_URL=https://rpc-amoy.polygon.technology/

# Optional: Infura API key (if using Infura instead of public RPC)
# INFURA_API_KEY=your_infura_api_key_here

# ============================================
# USDT CONTRACT ADDRESS
# ============================================
# USDT contract address on Polygon Amoy testnet
# You need to find the official USDT testnet contract address
# Check: https://docs.polygon.technology/docs/develop/network-details/network/
# Or use a test USDT token deployed on Amoy
NEXT_PUBLIC_USDT_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000

# ============================================
# ESCROW SMART CONTRACT
# ============================================
# Deploy TokenEscrow.sol to Polygon Amoy first, then add the deployed address here
# This is the contract that locks and releases USDT
NEXT_PUBLIC_ESCROW_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000

# ============================================
# SERVER PRIVATE KEY (CRITICAL - KEEP SECURE!)
# ============================================
# Private key of the wallet that deployed the escrow contract
# This wallet becomes the "owner" and can release tokens from escrow
# NEVER commit this to git! Only use in .env.local (which should be in .gitignore)
# Format: 0x followed by 64 hex characters (no 0x prefix needed, but accepted)
PRIVATE_KEY=your_private_key_here_without_0x_prefix

# ============================================
# RAZORPAY CONFIGURATION
# ============================================
# Get these from: https://dashboard.razorpay.com/app/keys
# Use test keys for development, production keys for production
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your_razorpay_secret_key_here

# ============================================
# CLERK AUTHENTICATION (Optional - for backward compatibility)
# ============================================
# If you're using Clerk for some auth features
# NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxxxxxxxxxxxx
# CLERK_SECRET_KEY=sk_test_xxxxxxxxxxxxx

# ============================================
# APPLICATION CONFIGURATION
# ============================================
# Base URL of your application (for webhooks, redirects, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ============================================
# WEBHOOK CONFIGURATION
# ============================================
# For Razorpay webhooks, you'll need to expose your local server
# Use ngrok or similar: ngrok http 3000
# Then add the ngrok URL in Razorpay dashboard webhook settings
# Example: https://abc123.ngrok.io/api/razorpay/webhook

# ============================================
# NOTES
# ============================================
# 1. Copy this file to .env.local and fill in your actual values
# 2. Never commit .env.local to git (it should be in .gitignore)
# 3. For production, use environment variables in your hosting platform
# 4. The PRIVATE_KEY must be the deployer of the escrow contract
# 5. Make sure your database is accessible from your application
